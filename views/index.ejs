<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Characters</h1>
    <ul>
        <% for (let i = 0; i < toons.length; i++){%>
            <li>
                <a href="/toons/<%= toons[i].id %>"><%= toons[i].name %></a>.</br>
                <p>Description: <%= toons[i].description %>.</p>
                <p>Price: $<%= toons[i].price %>.</p>
                <% if(toons[i].qty > 0) { %>
                    <p><%= toons[i].qty %> remaining.</p>
                    <% } else { %>
                    <p>OUT OF STOCK</p>
                <% } %>
                <a href="/toons/<%= toons[i].id %>/edit">Edit</a>
                <form action="/toons/<%= toons[i]._id%>?_method=DELETE" method="POST">
                    <input type="submit" value="Delete">
                </form>
            </li>
        <% } %>
    </ul>
    <a href="/toons/new">Enter a New Toon</a>
</body>
</html>