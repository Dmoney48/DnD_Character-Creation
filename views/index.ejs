<%- include('./partials/header.ejs') %>
    <h1>Characters</h1>
    <!-- <ul>
        <% for (let i = 0; i < toons.length; i++){%>
            <li>
                <a href="/toons/<%= toons[i].id %>"><%= toons[i].name %></a>.</br>
                <p><%= toons[i].description %>.</p>
               <div>
                <img src="<%= toons[i].img %>" alt="image">
               </div>
                <!-- <p><%= toons[i].str %></p> -->
                <!-- <a href="/toons/<%= toons[i].id %>/edit">Edit</a>
                <form action="/toons/<%= toons[i]._id%>?_method=DELETE" method="POST">
                    <input type="submit" value="Delete">
                </form>
                
            </li>
        <% } %>
    </ul> -->
    <div class="container">
    <div class="row">
    <div class="card-group">
        <% for (let i = 0; i < toons.length; i++){%>
            <div class="col-3">
                <div class="card">
                    <a href="/toons/<%= toons[i].id %>"><img src="<%= toons[i].img %>" class="card-img-top" alt="..."></a>
                     <div class="card-body">
                       <h5 class="card-title"><%= toons[i].name %></h5>
                       <p class="card-text"> <%= toons[i].class %> </p>
                       <p class="card-text"><small class="text-muted"><%= toons[i].description %></small></p>
                       <a href="/toons/<%= toons[i].id %>/edit">Edit</a>
                       <form action="/toons/<%= toons[i]._id%>?_method=DELETE" method="POST">
                           <input type="submit" value="Delete">
                       </form>
                     </div>
                   </div>
            </div>
        <% } %>
        </div>
    </div>
</div>
<%- include('./partials/footer.ejs')%>