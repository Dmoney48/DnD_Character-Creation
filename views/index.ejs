<%- include('./partials/header.ejs') %>
    <h1>Characters</h1>
    <!-- <ul>
        <% for (let i = 0; i < toons.length; i++){%>
            <li>
                <a href="/toons/<%= toons[i].id %>"><%= toons[i].name %></a>.</br>
                <p><%= toons[i].description %>.</p>
               <div>
                <img src="<%= toons[i].img %>" alt="image">
               </div>
                <!-- <p><%= toons[i].str %></p> -->
                <!-- <a href="/toons/<%= toons[i].id %>/edit">Edit</a>
                <form action="/toons/<%= toons[i]._id%>?_method=DELETE" method="POST">
                    <input type="submit" value="Delete">
                </form>
                
            </li>
        <% } %>
    </ul> -->
    <div class="row row-cols-3 row-cols-md-2 g-4">
    <div class="card-group">
        <% for (let i = 0; i < toons.length; i++){%>
        <div class="card">
          <img src="<%= toons[i].img %>" class="card-img-top" alt="...">
          <div class="card-body">
            <h5 class="card-title">Card title</h5>
            <p class="card-text">This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.</p>
            <p class="card-text"><small class="text-muted">Last updated 3 mins ago</small></p>
            <a href="/toons/<%= toons[i].id %>/edit">Edit</a>
            <form action="/toons/<%= toons[i]._id%>?_method=DELETE" method="POST">
                <input type="submit" value="Delete">
            </form>
          </div>
        </div>
        </div>
        <% } %>
    </div>
</div>
    <a href="/toons/new">Enter a New Toon</a>
<%- include('./partials/footer.ejs')%>